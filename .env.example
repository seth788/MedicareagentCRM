# Copy to .env.local and set your keys.

# Supabase (Project Settings → API in dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Optional: required only for scripts/migrate-encrypt-mbi.ts (one-time MBI encryption migration)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# 32-byte key for encrypting Medicare numbers (PHI). Generate with: openssl rand -hex 32
# Do not commit .env.local; set this in production secrets.
ENCRYPTION_KEY=

# Google Places (restrict by HTTP referrer for security)
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your_google_places_api_key
